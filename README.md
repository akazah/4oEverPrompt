# 4oEverPrompt

Challenge: reproduce GPT-4o-like responses using GPT-5.
(Inspired by #Keep4o)

- Currently, I am trying it out with ChatGPT5 Thinking.

## Linting (prompt-only)

This repository centers on a prompt. For checks, keep it to linting Markdown and the TypeScript that mirrors the prompt schema.

- Markdown: `markdownlint README.md 4oEverPrompt_ja.md`
- TypeScript: `eslint "src/**/*.ts" "tests/**/*.ts"`
